{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "Run Famco (dev + Chrome)",
      "command": "npm run dev",
      "serverReadyAction": {
        "pattern": "Local.+(https?://\\S+)",
        "uriFormat": "%s",
        "action": "openExternally"
      }
    }
  ]
}
