---
import { Icon } from "astro-icon/components";
import { asset, link } from "@/lib/asset";

const COLS = [
  { title: "Inventory", links: [
    { label: "View all stock", href: "/stock" },
    { label: "Trucks", href: "/stock?category=trucks" },
    { label: "Buses", href: "/stock?category=buses" },
    { label: "Construction equipment", href: "/stock?category=construction" },
    { label: "Material handling", href: "/stock?category=material-handling" },
    { label: "Power & industrial", href: "/stock?category=power" },
  ]},
  { title: "Buy & sell", links: [
    { label: "How to buy", href: "/stock" },
    { label: "0% interest finance", href: "/stock/volvo-fh-460-tractor#finance" },
    { label: "Trade in & trade up", href: "/stock?category=sell" },
    { label: "Get free valuation", href: "/stock?category=sell" },
    { label: "Live video inspection", href: "/stock/volvo-fh-460-tractor" },
  ]},
  { title: "Service", links: [
    { label: "TMH Workshop", href: "#" },
    { label: "Genuine parts", href: "#" },
    { label: "Book a service slot", href: "#" },
    { label: "GCC delivery", href: "#" },
    { label: "Customer support", href: "#" },
  ]},
  { title: "Company", links: [
    { label: "About Al-Futtaim Famco", href: "#" },
    { label: "Al-Futtaim Group", href: "#" },
    { label: "News & articles", href: "#" },
    { label: "Careers", href: "#" },
    { label: "Ethics & compliance", href: "#" },
  ]},
];

// Country tabs — switching tabs swaps which yards are visible on the map.
const COUNTRIES = [
  { code: "UAE",     label: "UAE",     flag: "ae" },
  { code: "KSA",     label: "Saudi",   flag: "sa" },
  { code: "Qatar",   label: "Qatar",   flag: "qa" },
  { code: "Bahrain", label: "Bahrain", flag: "bh" },
] as const;

// Three scattered pin positions per country (percentages within the right pane).
// Tuned for a Persian-Gulf-centered map at zoom 5 — all four GCC countries visible.
// Icons come from the same `/icons/category/` set used by the home-page CategoryStrip.
const PIN_POSITIONS_3: Record<
  string,
  { left: string; top: string; icon: string }[]
> = {
  KSA: [
    // Western mass: north-east, central (Riyadh), south
    { left: "12%", top: "30%", icon: "/icons/category/big-truck.svg" },             // Tractor heads
    { left: "22%", top: "50%", icon: "/icons/category/_construction-equipment.svg" },  // Construction
    { left: "18%", top: "70%", icon: "/icons/category/loader.svg" },                   // MHE
  ],
  Qatar: [
    // Peninsula extending north into the Gulf, just east of Bahrain
    { left: "52%", top: "52%", icon: "/icons/category/tipper.svg" },                   // Truck rigid
    { left: "56%", top: "56%", icon: "/icons/category/_construction-equipment.svg" },  // Construction
    { left: "50%", top: "60%", icon: "/icons/category/van.svg" },                      // Vans
  ],
  Bahrain: [
    // Small island just west of Persian Gulf centre
    { left: "42%", top: "42%", icon: "/icons/category/_trailer.svg" },                 // Trailers
    { left: "46%", top: "46%", icon: "/icons/category/van.svg" },                      // Vans
    { left: "40%", top: "50%", icon: "/icons/category/loader.svg" },                   // MHE
  ],
  UAE: [
    // Southeast end of the Gulf
    { left: "68%", top: "64%", icon: "/icons/category/big-truck.svg" },             // Tractor heads
    { left: "78%", top: "70%", icon: "/icons/category/tipper.svg" },                   // Truck rigid
    { left: "72%", top: "80%", icon: "/icons/category/_construction-equipment.svg" },  // Construction
  ],
};

const SOCIALS = ["lucide:linkedin", "lucide:facebook", "lucide:instagram", "lucide:youtube"] as const;
---

<footer class="bg-charcoal text-white">
  {/* ─── Branch network — full-bleed split background, container-aligned text ─── */}
  <section class="relative border-b border-white/10">
    {/* Full-bleed split background — charcoal left, gradient + map right */}
    <div class="absolute inset-0 grid grid-cols-1 lg:grid-cols-2">
      <div class="bg-charcoal-900"></div>
      <div class="relative bg-secondary-600 overflow-hidden">
        {/* Google Map base — grayscale so country names + outlines stay readable */}
        <iframe
          title="Al-Futtaim Famco yards across UAE, Saudi Arabia, Qatar and Bahrain"
          src="https://maps.google.com/maps?q=Persian+Gulf&t=&z=5&ie=UTF8&iwloc=&output=embed"
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"
          allowfullscreen
          class="absolute inset-0 h-full w-full border-0 grayscale contrast-110 opacity-30"
        ></iframe>

        {/* Yellow brand tint — multiply blend keeps map visible while on-brand */}
        <div class="absolute inset-0 bg-gradient-to-br from-secondary-300 via-secondary to-secondary-700 mix-blend-multiply pointer-events-none"></div>
        {/* Subtle vignette for depth — does NOT obscure the map */}
        <div class="absolute inset-0 bg-[radial-gradient(ellipse_70%_60%_at_95%_95%,rgba(8,8,11,0.35),transparent_60%)] pointer-events-none"></div>
      </div>
    </div>

    {/* Container-aligned content layer */}
    <div class="relative container">
      <div class="grid lg:grid-cols-2 min-h-[680px] lg:min-h-[760px]">
        {/* LEFT — heading + CTAs (aligned to container's left edge) */}
        <div class="py-16 lg:py-20 flex flex-col justify-center lg:pr-12 text-white">
          <h3 class="font-display text-3xl lg:text-5xl font-bold leading-[1.05] tracking-tight">
            Yards across the <span class="text-secondary-300">GCC</span>.
            <br />Picked by <span class="text-secondary-300">category</span>.
          </h3>
          <div class="mt-8 flex flex-wrap gap-3">
            <a
              href={link("/stock")}
              class="inline-flex items-center gap-2 btn btn-primary h-12 px-5 text-[14px] rounded-md"
            >
              View all stock
              <Icon name="lucide:arrow-right" class="h-4 w-4" />
            </a>
            <a
              href="tel:80032626"
              class="inline-flex items-center gap-2 h-12 px-5 rounded-md border border-white/15 bg-white/5 hover:bg-white/10 text-white text-[14px] font-semibold transition-colors"
            >
              <Icon name="lucide:phone" class="h-4 w-4 text-secondary-300" />
              800 32626
            </a>
          </div>
        </div>

        {/* RIGHT — 3 circular icon pins per country, scattered across the gradient */}
        <div class="relative hidden lg:block py-12 lg:py-14">
          <div class="absolute inset-0">
            {COUNTRIES.flatMap((c) =>
              PIN_POSITIONS_3[c.code].map((p, i) => (
                <span
                  aria-hidden="true"
                  class="absolute -translate-x-1/2 -translate-y-1/2 inline-flex h-12 w-12 rounded-2xl bg-white items-center justify-center ring-2 ring-charcoal/15 shadow-[0_10px_24px_rgba(8,8,11,0.35)] transition-transform hover:scale-110"
                  style={`left:${p.left};top:${p.top};`}
                >
                  <span
                    aria-hidden="true"
                    class="block h-7 w-7 bg-charcoal-900"
                    style={`-webkit-mask: url(${asset(p.icon)}) center / contain no-repeat; mask: url(${asset(p.icon)}) center / contain no-repeat;`}
                  />
                </span>
              ))
            )}
          </div>
        </div>
      </div>
    </div>

    {/* Mobile fallback — same minimal pin layout on a brand-tinted map */}
    <div class="lg:hidden relative z-10 container py-8">
      <div class="relative h-[280px] rounded-2xl bg-secondary-600 overflow-hidden ring-1 ring-secondary-700/30">
        <iframe
          title="Al-Futtaim Famco yards across UAE, Saudi Arabia, Qatar and Bahrain"
          src="https://maps.google.com/maps?q=Persian+Gulf&t=&z=5&ie=UTF8&iwloc=&output=embed"
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"
          allowfullscreen
          class="absolute inset-0 h-full w-full border-0 grayscale contrast-110 opacity-30"
        ></iframe>
        <div class="absolute inset-0 bg-gradient-to-br from-secondary-300 via-secondary to-secondary-700 mix-blend-multiply pointer-events-none"></div>
        <div class="absolute inset-0 bg-[radial-gradient(ellipse_70%_60%_at_95%_95%,rgba(8,8,11,0.35),transparent_60%)] pointer-events-none"></div>
        <div class="absolute inset-0">
          {COUNTRIES.flatMap((c) =>
            PIN_POSITIONS_3[c.code].map((p) => (
              <span
                aria-hidden="true"
                class="absolute -translate-x-1/2 -translate-y-1/2 inline-flex h-10 w-10 rounded-2xl bg-white items-center justify-center ring-2 ring-charcoal/15 shadow-[0_8px_20px_rgba(8,8,11,0.35)]"
                style={`left:${p.left};top:${p.top};`}
              >
                <span
                  aria-hidden="true"
                  class="block h-6 w-6 bg-charcoal-900"
                  style={`-webkit-mask: url(${asset(p.icon)}) center / contain no-repeat; mask: url(${asset(p.icon)}) center / contain no-repeat;`}
                />
              </span>
            ))
          )}
        </div>
      </div>
    </div>
  </section>

  <div class="border-b border-white/10">
    <div class="container py-10 lg:py-12">
      <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-6">
        <div>
          <h3 class="font-display text-2xl lg:text-3xl font-bold leading-tight">
            Subscribe for latest stock<br class="hidden sm:block" />
            and offers
          </h3>
        </div>

        <form action="#" method="post" class="flex w-full lg:w-auto lg:min-w-[440px] gap-2">
          <input
            type="email"
            required
            placeholder="Enter your email"
            aria-label="Email address"
            class="flex-1 h-12 px-4 rounded-md bg-white/5 border border-white/15 text-[14px] text-white placeholder:text-white/40 outline-none focus:border-secondary focus:bg-white/10 transition-colors"
          />
          <button
            type="submit"
            class="btn btn-primary h-12 px-5 text-[14px] rounded-md whitespace-nowrap"
          >
            Subscribe
            <Icon name="lucide:arrow-right" class="h-4 w-4" />
          </button>
        </form>
      </div>
    </div>
  </div>

  <div class="container py-12 lg:py-16">
    <div class="grid lg:grid-cols-[1.5fr_repeat(4,minmax(0,1fr))] gap-10 lg:gap-12">
      <div>
        <a href={link("/")} class="inline-flex items-center mb-5">
          <img
            src={asset("/brand/logo.png")}
            alt="Al-Futtaim FAMCO Used Equipment"
            class="h-16 w-auto brightness-0 invert"
          />
        </a>
        <p class="text-[13.5px] text-white/65 leading-relaxed mb-6 max-w-sm">
          Part of Al-Futtaim Group · UAE · 2026. Sole Volvo distributor —
          inspected used trucks, buses and machinery, delivered across the GCC.
        </p>

        <div class="space-y-2.5">
          <a
            href="tel:80032626"
            class="flex items-center gap-2.5 text-[13.5px] text-white/80 hover:text-secondary-300 transition-colors"
          >
            <span class="h-7 w-7 rounded-md bg-white/10 flex items-center justify-center shrink-0">
              <Icon name="lucide:phone" class="h-3.5 w-3.5" />
            </span>
            800 32626
          </a>
          <a
            href="mailto:FAMCO@alfuttaim.ae"
            class="flex items-center gap-2.5 text-[13.5px] text-white/80 hover:text-secondary-300 transition-colors"
          >
            <span class="h-7 w-7 rounded-md bg-white/10 flex items-center justify-center shrink-0">
              <Icon name="lucide:mail" class="h-3.5 w-3.5" />
            </span>
            FAMCO@alfuttaim.ae
          </a>
        </div>
      </div>

      {COLS.map((c) => (
        <div>
          <h4 class="text-[11px] uppercase tracking-[0.18em] font-bold text-white mb-4">
            {c.title}
          </h4>
          <ul class="space-y-2.5">
            {c.links.map((l) => (
              <li>
                <a
                  href={link(l.href)}
                  class="text-[13.5px] text-white/65 hover:text-secondary-300 transition-colors"
                >
                  {l.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>

  <div class="border-t border-white/10 bg-charcoal-800">
    <div class="container py-5 flex flex-col lg:flex-row lg:items-center lg:justify-between gap-4">
      <div class="flex items-center gap-2">
        {SOCIALS.map((iconName) => (
          <a
            href="#"
            aria-label="Social link"
            class="h-9 w-9 rounded-md bg-white/5 border border-white/10 hover:bg-secondary hover:border-secondary flex items-center justify-center transition-colors"
          >
            <Icon name={iconName} class="h-4 w-4" />
          </a>
        ))}
      </div>

      <div class="flex flex-wrap items-center gap-x-5 gap-y-1.5 text-[12.5px] text-white/60">
        <span>© 2026 Al-Futtaim Auto &amp; Machinery Co. LLC</span>
        <a href="#" class="hover:text-white">Privacy</a>
        <a href="#" class="hover:text-white">Cookies</a>
        <a href="#" class="hover:text-white">Terms</a>
        <a href="#" class="hover:text-white">Sitemap</a>
      </div>

      <button class="inline-flex items-center gap-2 self-start lg:self-auto bg-white/5 border border-white/10 text-[13px] font-medium text-white px-3.5 py-2 rounded-md hover:bg-white/10 transition-colors">
        <span aria-hidden="true" class="text-base leading-none">🇦🇪</span>
        English
        <Icon name="lucide:chevron-down" class="h-3.5 w-3.5 opacity-70" />
      </button>
    </div>
  </div>
</footer>
